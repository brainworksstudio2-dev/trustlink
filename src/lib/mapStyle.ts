// Single source of truth for the MapLibre style URL. OpenFreeMap is free,
// unlimited, and needs no API key — swap this one constant to change
// providers later (self-hosted OpenFreeMap, MapTiler, Stadia, etc.) without
// touching any screen.
export const MAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/liberty';
